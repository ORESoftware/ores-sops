name: Apply stage contract fixes

on:
  push:
    branches:
      - feat/stage-environment-contract-v0.4

permissions:
  contents: write

concurrency:
  group: apply-stage-fix-${{ github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[stage-fix-applied]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: true

      - name: Apply fixes
        run: python3 scripts/fix-stage-contract.py

      - name: Validate
        shell: bash
        run: |
          set -euo pipefail
          bash -n ores-sops
          git diff --check

      - name: Remove one-time fix machinery
        run: rm scripts/fix-stage-contract.py .github/workflows/apply-stage-fix.yml

      - name: Commit fixes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'ORESoftware stage contract automation'
          git config user.email 'bot@oresoftware.dev'
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'fix: preserve exact SOPS rules and safe heredocs [stage-fix-applied]'
          git push origin HEAD
